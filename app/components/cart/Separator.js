/* eslint-disable linebreak-style */
import { View } from 'react-native';
import React from 'react';
import styles from './styles';

// Separating the list items
const Separator = () => <View style={styles.Separator} />;
export default Separator;
